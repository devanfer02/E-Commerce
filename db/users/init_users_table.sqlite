CREATE TABLE "users" (
    id INTEGER PRIMARY KEY AUTOINCREMENT UNIQUE,
    first_name TEXT,
    last_name TEXT,
    email TEXT,
    phone_number TEXT,
        type TEXT
    );

CREATE INDEX user_id_idx ON users (id);