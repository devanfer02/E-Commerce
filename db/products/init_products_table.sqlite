CREATE TABLE "products" (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    seller_id INTEGER,
    FOREIGN KEY (seller_id) REFERENCES Users(id),
    title TEXT,
    description TEXT,
    price TEXT,
    stock INTEGER
);